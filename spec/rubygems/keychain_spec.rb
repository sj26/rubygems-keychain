RSpec.describe Rubygems::Keychain do
  it "has a version number" do
    expect(Rubygems::Keychain::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
